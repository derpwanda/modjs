age = 10
console.log(age)
var age

//var based variable are function scoped, not block scoped
//var based variables can be declared multiple times
//var can be accessed before it's declared. 
//The declaration (not value/assignment) gets moved to the top of the scope.